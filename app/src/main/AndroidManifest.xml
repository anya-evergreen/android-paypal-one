<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.paypalsdkintegration">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PaypalSdkIntegration">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PaypalSdkIntegration.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.paypal.authcore.authentication.TokenActivity"
            android:exported="false"
            tools:node="merge" />
        <activity
            android:name="com.paypal.openid.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="merge" />
        <activity
            android:name="com.paypal.pyplcheckout.home.view.activities.PYPLHomeActivity"
            android:exported="false"
            tools:node="merge" />
        <activity
            android:name="com.paypal.pyplcheckout.home.view.activities.PYPLInitiateCheckoutActivity"
            android:exported="true"
            tools:node="merge" />
    </application>

</manifest>